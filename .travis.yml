language: scala
scala:
  - 2.11.12

jdk:
  oraclejdk8

stages:
  - name: test

    # - name: release
    #    if: (branch = master AND type = push) OR (tag IS present)

jobs:
  include:
    - stage: test
      script: sbt compile coverage test

before_install:
  - git fetch --tags

after_success:
  - sbt coveralls
